<Window x:Class="OS.Presentation.UI.Views.DemoGuideDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Demo Mode Guide" 
        Height="300" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F0F0F0">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#CCCCCC" BorderThickness="1" Background="White" Margin="10">
        <StackPanel Margin="20">
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="ℹ️" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="Demo Mode" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333333"/>
            </StackPanel>

            <!-- Module Info -->
            <Border Background="#E8F4FD" BorderBrush="#B3D7FF" BorderThickness="1" Padding="10" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Module:" FontWeight="SemiBold" Foreground="#0066CC" FontSize="12"/>
                    <TextBlock x:Name="ModuleNameText" Text="" FontWeight="Bold" Foreground="#0066CC" FontSize="14" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Function Info (if applicable) -->
            <Border x:Name="FunctionInfoBorder" Background="#FFF4E8" BorderBrush="#FFD799" BorderThickness="1" Padding="10" Margin="0,0,0,15" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Function:" FontWeight="SemiBold" Foreground="#CC6600" FontSize="12"/>
                    <TextBlock x:Name="FunctionNameText" Text="" FontWeight="Bold" Foreground="#CC6600" FontSize="14" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Description -->
            <TextBlock Text="Description:" FontWeight="SemiBold" Foreground="#333333" FontSize="12" Margin="0,0,0,5"/>
            <Border Background="#F8F8F8" BorderBrush="#DDDDDD" BorderThickness="1" Padding="12" Margin="0,0,0,15">
                <TextBlock x:Name="DescriptionText" Text="" TextWrapping="Wrap" Foreground="#555555"/>
            </Border>

            <!-- Demo Mode Notice -->
            <Border Background="#FFF0F0" BorderBrush="#FF9999" BorderThickness="1" Padding="10" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔒" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="This feature is read-only in Demo Mode." Foreground="#CC0000" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Close Button -->
            <Button Content="Close" HorizontalAlignment="Right" Click="OnCloseClick" Background="#0066CC" Foreground="White" BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0052A3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</Window>
