<Page x:Class="OS.Presentation.UI.Views.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Products">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="HeaderText" 
                      Text="Products" 
                      FontSize="28" 
                      FontWeight="Bold"
                      VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:TextBox x:Name="SearchBox" 
                           Width="250" 
                           PlaceholderText="Search products..."
                           Margin="0,0,10,0"
                           TextChanged="SearchBox_TextChanged"/>
                <ui:Button x:Name="AddButton" 
                            Content="Add Product" 
                            Appearance="Primary"
                            Icon="Add24"
                            Click="AddButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- Stock Alert Banner -->
        <ui:InfoBar Grid.Row="1" 
                   x:Name="StockAlert"
                   IsOpen="False"
                   Severity="Warning"
                   Title="Low Stock Alert"
                   Message="Some products are below minimum stock level."
                   Margin="0,0,0,10"/>
        
        <!-- Data Grid -->
        <ui:Card Grid.Row="2">
            <DataGrid x:Name="ProductsDataGrid"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="ColCode" Header="Code" Binding="{Binding Code}" Width="100"/>
                    <DataGridTextColumn x:Name="ColName" Header="Name" Binding="{Binding Name}" Width="250"/>
                    <DataGridTextColumn x:Name="ColCategory" Header="Category" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn x:Name="ColPrice" Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn x:Name="ColStock" Header="Stock" Binding="{Binding StockQuantity}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                        <Setter Property="Foreground" Value="#ff6b6b"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn x:Name="ColMinStock" Header="Min." Binding="{Binding MinStockLevel}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridCheckBoxColumn x:Name="ColActive" Header="Active" Binding="{Binding IsActive}" Width="70"/>
                    <DataGridTemplateColumn x:Name="ColActions" Header="Actions" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Button Appearance="Transparent" 
                                              Icon="Edit24" 
                                              Click="EditButton_Click"
                                              Tag="{Binding Id}"/>
                                    <ui:Button Appearance="Transparent" 
                                              Icon="Delete24" 
                                              Click="DeleteButton_Click"
                                              Tag="{Binding Id}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ui:Card>
    </Grid>
</Page>
